// Types is just a name It can be anything (e.g State, Props)

type Types = {
    className?: string,
    label?: string,
    fullWidth?: any,
    placeholder?: string,
    value?: string | number,
    onChange?: any,
    sx?: {},
    focused: boolean,
  }
export default Types;