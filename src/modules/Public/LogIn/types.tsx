// Types is just a name It can be anything (e.g State, Props)

type Types = {
    mobile?: number | null,
    email: string,
    password: string,
    showPassword: boolean,
  }
  export default Types;